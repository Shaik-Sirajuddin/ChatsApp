<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="1dp"
    android:layout_gravity="end"
    >

    <ImageView
        android:visibility="gone"
        android:background="@drawable/send_msg_back"
        android:padding="5dp"
        android:id="@+id/Image"
        android:layout_width="200dp"
        android:layout_height="130dp"
        android:scaleType="centerCrop"
        android:src="@drawable/profile"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView

        android:background="@drawable/send_msg_back"
        android:id="@+id/sender_msg"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@color/black"
        android:paddingStart="10dp"
        android:paddingTop="5dp"
        android:paddingEnd="60dp"
        android:paddingBottom="5dp"
        android:text="TextView"
        android:textSize="18sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@id/Image" />
    <TextView
        android:id="@+id/msg_time"
        android:layout_width="58dp"
        android:layout_height="23dp"
        android:padding="4dp"
        app:layout_constraintEnd_toEndOf="parent"
        android:text="12:59 am"
        android:textStyle="italic"
        android:textAlignment="center"
        android:textSize="12sp"
        app:layout_constraintBottom_toBottomOf="parent"
        />
    <ImageView
        android:visibility="gone"
        android:id="@+id/emoji"
        android:layout_width="16dp"
        android:layout_height="16dp"
        android:src="@drawable/heart"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@+id/sender_msg"
        app:layout_constraintStart_toStartOf="@id/sender_msg"
        app:layout_constraintTop_toTopOf="@+id/sender_msg" />
</androidx.constraintlayout.widget.ConstraintLayout>